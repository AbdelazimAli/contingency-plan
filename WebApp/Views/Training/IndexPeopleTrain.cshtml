


@using WebApp.Extensions

@{

    ViewBag.Title = "Index";
    Layout = null;
}
@{
    ViewBag.Title = "Index";
    Layout = null;
    var rtl = User.Identity.RTL();
    var admin = User.IsInRole("Admin");
    int dataLevel = 4, read = 0;
    int.TryParse(Request.QueryString["DataLevel"], out dataLevel);
    int.TryParse(Request.QueryString["Read"], out read);
}
<br>
<div id="TrainingCourseDiv"></div>

<script>

    var objectName = "TrainCourses";
   Grids.indexGrid({
        read: "@Url.Action("GetTrainingCourse", "Training")",
       edit: "@Url.Action("Details", "Training")",
       del: "@Url.Action("DeleteTrainCourse", "Training")",
       show: "@Url.Action("Details", "Training")",
       lang: "@User.Identity.GetCulture()",
        objectName: objectName,
        recordName: "Name",
        gridName: "TrainingCourseDiv",
        admin: "@admin",
          });
</script>

@Html.Partial("_IndexGrid", new ViewDataDictionary {
    { "AddRecord", @Url.Action("Details", "Training") },
      { "Admin", admin}, { "GridName", "TrainingCourseDiv" },{"objectName" , "TrainCourses"}, {"Functions",ViewBag.Functions} })















